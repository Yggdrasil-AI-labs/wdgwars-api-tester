# wdgwars-api-tester has no runtime dependencies — pure stdlib Python 3.
# This file exists so dependency-scanning tooling (and the CI install step)
# does not get confused by its absence. If you add a runtime dep, pin it here.
